{% extends 'base.html.twig' %}

{% block title %}Nos Produits{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/product/product_list.css') }}">
{% endblock %}

{% block body %}

    <h1>Nos produits</h1>

    <section class="products-container">
        {% for product in products %}
    <a href="{{ path('product_single', {'alias' : product.alias}) }}">
        <article class="product-card">
            <div class="img-container">

                {% for productCategory in product.category %}
                    {# | imagine_filter('my_thumb') => LiipImagine #}
                    <img src="{{ asset('images/' ~ productCategory.alias ~ '/mini/' ~ product.photo) }}"
                         alt="Photo du produit {{ product.label }}">
                {% endfor %}

                <a href="{{ path('cart_index', {'id' : product.id}) }}">
                    <i class="fa-solid fa-basket-shopping pointer icon1"></i>
                </a>
                <a href="{{ path('cart_add', {'id' : product.id}) }}">
                    <i class="fa-solid fa-plus icon3"></i>
                </a>
                <a href="{{ path('cart_remove', {'id' : product.id}) }}">
                    <i class="fa-solid fa-minus icon4"></i>
                </a>


                <a href="">
                    {# <i class="fa-solid fa-trash-can icon2"></i> #}
                </a>

                <a href="">
                    {# <i class="fa-solid fa-circle-plus icon3"></i> #}
                </a>

                <a href="{{ path('cart_remove', {'id' : product.id}) }}">
                    <i class="fa-solid fa-circle-minus icon4"></i>
                </a>
            </div>
            <div class="description-container">
                <div class="description">
                    <div class="producer">
                        <p>{{ product.producers.get(0) }}</p>
                    </div>
                    <div class="produit">
                        <p>
                            <a href="{{ path('product_single', {alias: product.alias}) }}">{{ product.label }}</a>
                        </p>
                    </div>
                </div>
                <div class="prices">
                    {% for productConditioning in product.conditioning %}
                        <span>{{ productConditioning.label }}</span>
                        <span>{{ product.unitPrice / productConditioning.conversionFactor }} / kg</span>
                    {% endfor %}
                    <span>Total</span>
                    <p>{{ product.unitPrice }} €</p>
                </div>
            </div>
        </article>
        </a>
        {% endfor %}
    </section>

    <div class="pagination">
        {{ knp_pagination_render(products) }}
    </div>

    <a href="#top">
        <i class="fa-solid fa-arrow-up anchor"></i>
    </a>

{% endblock %}
{# ----------------------------------------------------------------------------------------------------- #}

<div class="container-xl">
        <div class="row">
            <div class="col-md-3">
                <h2>Filtrer</h2>
                {{ form(form) }}
            </div>
            <div class="col-md-9">
                    <h1>Nos produits</h1>
            {% for product in products %}
                <div class="our-products-category">

                        <a href="{{ path('product_single', {alias: product.alias}) }}">
                            <div class="card-product">
                                <div class="card-product-img">

                                    {% for productCategory in product.category %}
                                    <img src="{{ asset('images/' ~ productCategory.alias ~ '/mini/' ~ product.photo) }}" alt="Photo du produit {{ product.label }}">
                                    {% endfor %}

                                </div>
                                <div class="card-product-body">
                                    <h4 class="product-name">{{ product.label }}</h4>
                                    <p><span class="card-product-producer">{{ product.producers.get(0) }}</span></p>

                                    {% for productConditioning in product.conditioning %}
                                        <p>{{ productConditioning.label }}</p>
                                        <p>{{ product.unitPrice / productConditioning.conversionFactor }} / kg</p>
                                    {% endfor %}

                                </div>
                                <div class="card-product-cart">
                                    <p>{{ product.unitPrice }}  €</p>

                                    <a href="{{ path('cart_index', {'id' : product.id})}}"><i class="fa-solid fa-basket-shopping pointer icon1"></i></a>
                                    <a href="{{ path('cart_add', {'id' : product.id})}}"><i class="fa-solid fa-plus icon3"></i></a>
                                    <a href="{{ path('cart_remove', {'id' : product.id})}}"><i class="fa-solid fa-minus icon4"></i></a>

                                </div>
                            </div>
                        </a>
                </div>
            {% endfor %}
        </div>
    </div>
{# ----------------------------------------------------------------------------------------------------- #}


        <div class="container-xl">
        <div class="row">
            <div class="col-md-3">
                <h2>Filtrer</h2>
                {{ form(form) }}